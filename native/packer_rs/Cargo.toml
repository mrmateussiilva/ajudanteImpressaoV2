[package]
name = "packer_rs"
version = "0.1.0"
edition = "2021"
description = "Native packing algorithms for Studio de Impressao"
license = "MIT"

[lib]
name = "packer_rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
