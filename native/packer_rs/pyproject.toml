[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "packer-rs"
version = "0.1.0"
description = "Rust acceleration module for Studio de Impressao"
requires-python = ">=3.13"

[tool.maturin]
module-name = "packer_rs"
bindings = "pyo3"
